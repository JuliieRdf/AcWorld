<p style="color: green"><%= notice %></p>

<h1>People</h1>

<div id="people">
  <% @people.each do |person| %>
    <%= render person %>
    <p>
    <button type="button" class="btn btn-primary" style="color:white"> <%= link_to "Show this person", person %></button>
    </p>
  <% end %>
</div>

<%= link_to "New person", new_person_path %>
