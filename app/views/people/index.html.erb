<p style="color: green"><%= notice %></p>

<h1 style="display:flex; justify-content: center">Welcome to our website</h1>
<div id="people">
  <% @people.each do |person| %>
    <%= render person %>
    <p>
    <button type="button" class="btn btn-primary" style="color:white"> <%= link_to "Show this person", person %></button>
    </p>
  <% end %>
</div>

<button type="button" class="btn btn-primary" style="color:white"><%= link_to "New person", new_person_path %> </button>
